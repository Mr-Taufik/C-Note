pop_back() 用于删除 vector 中的最后一个元素。调用后，vector 的大小减 1，但并不会减少 vector 的容量。如果你需要减少容量，可以使用 shrink_to_fit()。

std::vector<int> vec = {1, 2, 3};
vec.pop_back();  // 删除最后一个元素（3）

std::cout << "Size after pop_back: " << vec.size() << std::endl;  // 输出2





























































